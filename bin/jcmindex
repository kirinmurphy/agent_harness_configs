#!/usr/bin/env bash
set -euo pipefail

target="${1:-$PWD}"

if [[ -f "${target}" ]]; then
  exec uvx jcodemunch-mcp index-file --no-ai-summaries "$(cd "$(dirname "${target}")" && pwd)/$(basename "${target}")"
fi

exec uvx jcodemunch-mcp index --no-ai-summaries "${target}"
